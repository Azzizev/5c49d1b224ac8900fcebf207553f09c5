<resources>
    
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>

        <!-- enable content window transition -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- enable customization of calendar theme by modifying myCalendarDialogTheme -->
        <item name="android:datePickerDialogTheme">@style/myCalendarDialogTheme</item>
    </style>

    <style name="app_name_text">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/app_name</item>
        <item name="android:textSize">50dp</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:transitionName">appNameTransition</item>
        <item name="android:fontFamily">@font/berkshire_swash</item>
    </style>

    <style name="signup_editText">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textColorHint">@color/colorHint</item>
        <item name="android:textSize">14dp</item>
        <item name="android:layout_marginTop">3dp</item>
    </style>
    <style name="signup_alerts">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorRed</item>
        <item name="android:textSize">11dp</item>
    </style>
    
    <style name="myCalendarDialogTheme" parent="android:Theme.Material.Light.Dialog">
        <item name="android:datePickerStyle">@style/MyDatePickerStyle</item>
        <item name="android:colorAccent">@color/colorAccent</item>
    </style>
    <style name="MyDatePickerStyle" parent="@android:style/Widget.Material.Light.DatePicker">
        <item name="android:calendarTextColor">@android:color/black</item>
    </style>
        
    <style name="login_phase_button">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">14dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="myTheme" parent="AppTheme" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="StartBackground" parent="AppTheme">
        <item name="android:windowBackground">@drawable/background_v2</item>
    </style>
</resources>
